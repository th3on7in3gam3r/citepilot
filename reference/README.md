# Reference HTML

`saved-page.html` is the original single-file export (~814 KB, minified). It was saved from a live Next.js marketing site and is kept for design reference only.

The active product is **CitePilot** — see the root README.

It cannot run standalone because it depends on `/_next/static/*` assets that were not included in the export.
