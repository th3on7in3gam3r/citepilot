/** Fleet API + export requests per rolling hour per user. */
export const FLEET_API_RATE_LIMIT_PER_HOUR = 120;

/** Max API keys per Fleet user. */
export const FLEET_API_KEYS_MAX = 5;

export const FLEET_API_KEY_PREFIX = "cp_fleet_";
