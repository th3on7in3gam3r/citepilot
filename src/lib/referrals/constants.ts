/** Max free months a referrer can earn from conversions. */
export const MAX_REFERRAL_CREDITS = 6;

export const REFERRAL_COOKIE = "citepilot_ref";

export const REFERRAL_COOKIE_MAX_AGE_SEC = 30 * 24 * 60 * 60;

export const REFERRAL_CODE_LENGTH = 8;

export const REFERRAL_CODE_PATTERN = /^[A-Z0-9]{8}$/;

export const REFERRAL_SHARE_COPY =
  "I've been tracking my brand citations on ChatGPT with @CitePilot — it's the first GEO tool that actually proves lift. Get a free audit:";
